# Django Settings
DJANGO_SECRET_KEY=your-secret-key-here
DJANGO_DEBUG=1

# Email Configuration
# SMTP do Gmail para envio REAL de emails localmente
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=tucupilabs@gmail.com
EMAIL_HOST_PASSWORD=sua-senha-de-app-aqui

# Para debug (imprime no terminal ao invés de enviar):
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Site URL (usado em links nos emails)
SITE_URL=http://localhost:8000

# Default From Email
DEFAULT_FROM_EMAIL=Tucupi Labs <tucupilabs@gmail.com>
